---

variables:
  GIT_STRATEGY: clone
  GIT_DEPTH: 0
  GIT_SUBMODULE_STRATEGY: recursive
  GIT_SUBMODULE_DEPTH: 1
  # VM_HOST Pool:
  VM_START_NUM: "001"
  VM_END_NUM: "016"

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'

stages:
  - pre-test
  - test

.do_build:
  # https://stackoverflow.com/questions/62689235/gitlab-ci-how-to-ignore-directory-using-rules-syntax
  rules:
    - changes:
        - .ci/
        - packaging/
        - go/
        - rpmbuild/
      when: never
    - when: on_success
  tags: [k8s, linux, x86_64]
  # tags: [k8s, mtr-rdma, x86_64]

unittests-build-job:
  tags: [k8s, linux, x86_64]
  stage: pre-test
  image: "$FEDORA_IMAGE"
  variables:
    XLIO_SHA: 3.51.1
  parallel:
    matrix:
      - BUILD_TYPE: verbs-debug
        FLAGS: --with-rdma --enable-debug --enable-werror
      - BUILD_TYPE: mlx5_dv-debug
        FLAGS: --with-rdma=mlx5_dv --enable-debug --enable-werror
      - BUILD_TYPE: mlx5_dv-release
        FLAGS: --with-rdma=mlx5_dv --enable-werror
  script:
    - .ci/install_xlio.sh $XLIO_SHA
    - ./configure ${FLAGS} --with-crypto --with-vfio-user --with-xlio=/tmp/libxlio/install --with-shared
    - make -j4
    - ./test/unit/unittest.sh

test-nvmf-job:
  tags: [vm_host, x86_64]
  stage: test
  before_script:
    - .ci/prepare_vm.sh
  script:
    - ./configure --enable-debug --with-rdma=mlx5_dv
    - make -j${nproc}
    - ./scripts/setup.sh
    - NET_TYPE=phy ./test/nvmf/host/bdevperf.sh --transport=rdma
    - NET_TYPE=phy ./test/nvmf/host/discovery.sh --transport=rdma
    - NET_TYPE=phy ./test/nvmf/host/identify.sh --transport=rdma

autotest-vm_host-matrix:
  tags: [vm_host, x86_64]
  stage: test
  parallel:
    matrix:
      - TEST_TYPE: [nvme, accel, fsdev]
  before_script:
    - .ci/prepare_vm.sh
  script:
    - ./autorun.sh .ci/autotest-${TEST_TYPE}.conf
  after_script:
    - tar czf $CI_PROJECT_DIR/${TEST_TYPE}_artifacts.tar -C $CI_PROJECT_DIR/.. output
  artifacts:
    name: ${TEST_TYPE}_artifacts
    paths:
      - $CI_PROJECT_DIR/${TEST_TYPE}_artifacts.tar
    expire_in: 3 days
    when: always

test-crypto-job:
  tags: [vm_host, x86_64]
  stage: test
  variables:
    # VMs 17-20 have BlueField3 vfs with crypto
    VM_START_NUM: "017"
    VM_END_NUM: "020"
    CI_DUT_RDMA: "mlx5_0"
  before_script:
    - .ci/prepare_vm.sh
  script:
    - ./configure --enable-debug --with-rdma=mlx5_dv --with-crypto
    - make -j${nproc}
    - ./scripts/setup.sh
    - ./test/accel/mlx5/accel_mlx5_crypto_key_upper_lba.sh --allowed-devs=${CI_DUT_RDMA}
    - ./test/accel/mlx5/accel_mlx5_crypto.sh --allowed-devs=${CI_DUT_RDMA}
    - ./test/accel/mlx5/accel_mlx5_crypto.sh --allowed-devs=${CI_DUT_RDMA} --transport=rdma
  after_script:
    - tar czf $CI_PROJECT_DIR/crypto_artifacts.tar -C $CI_PROJECT_DIR/.. output
  artifacts:
    name: crypto_artifacts
    paths:
      - $CI_PROJECT_DIR/crypto_artifacts.tar
    expire_in: 3 days
    when: always

coverity-job:
  tags: [k8s, linux, x86_64]
  stage: test
  image: "$COV_IMAGE"
  script:
    - apt-get -q update
    - ./scripts/pkgdep.sh
    - ./configure --with-rdma=mlx5_dv --disable-unit-tests
    - cov-build --dir cov_build make -j 4
    - cov-analyze --jobs auto --security --concurrency --dir cov_build
    - cov-format-errors --dir cov_build --html-output cov_build/html
    - nr=$(cov-format-errors --dir cov_build |& awk '/Processing [0-9]+ errors?/ { print $2 }')
    - exit $nr
  variables:
    PATH: /usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/cov/bin
    CC: gcc-9
  allow_failure: true
  artifacts:
    name: coverity
    paths:
      - cov_build/html/
    expire_in: 1 week
    when: always

code-style-job:
  extends: .do_build
  stage: pre-test
  image: "$COV_IMAGE"
  script:
    - apt-get -q update
    - ./scripts/pkgdep.sh --developer-tools
    - ./scripts/check_format.sh

check-so-deps-job:
  tags: [vm_host, x86_64]
  stage: test
  variables:
    GIT_FETCH_EXTRA_FLAGS: "--tags"
  script:
    - ./configure --with-rdma=mlx5_dv --enable-debug --enable-werror --with-shared
    - make -j4
    - git clone https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.com/Mellanox/spdk_team/spdk-abi.git test/make/abi
    - sudo ./test/make/check_so_deps.sh

# Disable temporarily the job, need additional debugging on ubuntu
# external-code-test-job:
#   extends: .test-job-vm-template
#   tags: [not-crypto, vm, linux, x86_64, f39]
#   script:
#     # clean up output from previous runs
#     - sudo rm -rf $CI_PROJECT_DIR/../output/
#     # clean up previous build to have a clean new one
#     - rm -rf ./mk/config.mk
#     - rm -rf ./build
#     - ./test/external_code/test_make.sh ./

test-virtio-fs-app:
  stage: test
  tags: [k8s, mtr-rdma, x86_64]
  image: "$FEDORA_IMAGE"
  variables:
    VIRTIO_FS_TARGET_REF: main
    GIT_STRATEGY: none
  script:
    - |
      if [ x"$VIRTIO_FS_CI_WEBREPO_URL" != x"" ]; then
        REF_NAME=$(echo "$CI_COMMIT_REF_NAME" | tr '/' '_')
        wget $VIRTIO_FS_CI_WEBREPO_URL/ci-$REF_NAME.json || true
        if [ -f ci-$REF_NAME.json ]; then
          VIRTIO_FS_TARGET_REF=$(cat ci-$REF_NAME.json | jq .spdk.app_ref)
          echo ci-$REF_NAME.json found! Redefined VIRTIO_FS_TARGET_REF=$VIRTIO_FS_TARGET_REF
        else
          echo ci-$REF_NAME.json not found! Using VIRTIO_FS_TARGET_REF=$VIRTIO_FS_TARGET_REF
        fi
      fi
    - >
      curl -s --header "PRIVATE-TOKEN: $VIRTIO_FS_TARGET_API_TOKEN" --request POST
      --form token=$VIRTIO_FS_TARGET_TRIGGER_TOKEN --form ref=$VIRTIO_FS_TARGET_REF
      --form variables[SPDK_REPO_URL]="$CI_REPOSITORY_URL" --form variables[SPDK_SHA]="$CI_COMMIT_SHA"
      --form variables[SPDK_PIPELINE_ID]="$CI_PIPELINE_ID" --form variables[SPDK_PIPELINE_URL]="$CI_PIPELINE_URL"
      --form variables[SPDK_JOB_ID]="$CI_JOB_ID" --form variables[SPDK_JOB_URL]="$CI_JOB_URL"
      $VIRTIO_FS_TARGET_API_V4_URL/projects/$VIRTIO_FS_TARGET_PROJECT_ID/trigger/pipeline > ./trigger.json
    - |
      VIRTIO_FS_PIPELINE_ID=$(cat ./trigger.json | jq .id)
      VIRTIO_FS_PIPELINE_URL=$(cat ./trigger.json | jq -r .web_url)
      echo virtio-fs-target app pipeline $VIRTIO_FS_PIPELINE_ID triggered. Pipeline URL: $VIRTIO_FS_PIPELINE_URL
      echo Waiting for the pipeline to finish...
      while true; do
        sleep 10
        curl -s --header "PRIVATE-TOKEN: $VIRTIO_FS_TARGET_API_TOKEN" $VIRTIO_FS_TARGET_API_V4_URL/projects/$VIRTIO_FS_TARGET_PROJECT_ID/pipelines/$VIRTIO_FS_PIPELINE_ID > ./pipeline.json
        PIPELINE_STATUS=$(cat ./pipeline.json | jq -r .status)
        case $PIPELINE_STATUS in
          created | waiting_for_resource | preparing | pending | running)
            echo "Pipeline $VIRTIO_FS_PIPELINE_ID current status: $PIPELINE_STATUS"
            ;;
          *)
            echo "Pipeline $VIRTIO_FS_PIPELINE_ID finished with status: $PIPELINE_STATUS"
            break
            ;;
        esac
      done
    - |
      PIPELINE_STATUS=$(cat ./pipeline.json | jq -r .status)
      if [ "$PIPELINE_STATUS" != "success" ]; then
          echo "Pipeline $VIRTIO_FS_PIPELINE_ID failed with status: $PIPELINE_STATUS"
          false
      fi
  dependencies: []
  artifacts:
    when: always
    paths:
      - trigger.json
      - pipeline.json
